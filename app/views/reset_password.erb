<div class='container'>
	<h3><%=@mbr.fname%> <%=@mbr.lname%>, <%=@mbr.callsign%>
	<br/>
	Your temporary password has expired, please reset it with a new one</h3>
</div>
<div>
	<form action='/reset_password' onsubmit="return validatePassword()" method='POST'>
		<input type="password" id="password" name="password" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" title="Must contain at least one number and one uppercase and lowercase letter, and at least 8 or more characters" placeholder="Password" required>
		<input name="confirm_password" required="required" type="password" id="confirm_password" placeholder="Confirm Password"/>
		<input type="hidden" id="mbr_id" value=<%="#{@mbr.id}"%> name="mbr_id">
		<input type="submit" value="Submit"/>
	</form>
</div>
<div id="message">
  <h3>Password must contain the following:</h3>
  <p id="letter" class="invalid">A <b>lowercase</b> letter</p>
  <p id="capital" class="invalid">A <b>capital (uppercase)</b> letter</p>
  <p id="number" class="invalid">A <b>number</b></p>
  <p id="length" class="invalid">Minimum <b>8 characters</b></p>
</div>